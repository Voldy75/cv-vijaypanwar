# RAG — Supabase (Dashboard → Project Settings → API)
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJxxx

# RAG — OpenAI Embeddings (platform.openai.com → API Keys)
OPENAI_API_KEY=sk-xxx

# Chatbot — Vercel AI Gateway (Gemini)
# Vercel dashboard → AI Gateway → API Keys.
# On Vercel deployments this can resolve automatically via OIDC.
AI_GATEWAY_API_KEY=vck_xxx
